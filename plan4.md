Wait, if we are the Platform (Hanfledge LMS), and we are launching an external LTI 1.3 Tool, then the config values in `LTI13Adapter` make sense if they point to the Tool's endpoints!
Wait! The LTI 1.3 Adapter struct:
```go
type LTI13Adapter struct {
	clientID     string
	deploymentID string
	platformURL  string
	keysetURL    string
	authURL      string
	tokenURL     string
	privateKey   string
}
```
If we are the Platform, what is `platformURL`? The Platform's own URL. Why would we store our own URL in the adapter instance config?
If we are the Tool, `platformURL` is the Platform's issuer URL. `keysetURL` is the Platform's JWKS. `authURL` is the Platform's OIDC endpoint. `tokenURL` is the Platform's access token endpoint. This is exactly how a Tool configures an LTI 1.3 connection to a Platform!

Wait, let's look at `LaunchURL` return type again:
```go
type LaunchResponse struct {
	URL       string            `json:"url"`
	Method    string            `json:"method"` // "GET" or "POST"
	FormData  map[string]string `json:"form_data,omitempty"`
	SessionID string            `json:"session_id"`
}
```
If our system is the Tool, and we are generating a `LaunchURL` for a specific learning activity... why would we generate a LaunchURL to the *Platform*? We don't! The user starts in the Platform and launches into the Tool!
BUT wait, there are two types of LMS integrations.
1. The Tool connects to the LMS.
2. The LMS connects to external tools (using LTI).
Hanfledge is an "AI-Native EdTech platform". So it's probably the LMS (Platform).
If Hanfledge is the LMS (Platform), it needs to launch external LTI 1.3 tools.
So `LTI13Adapter` config contains the configuration to talk to the Tool.
But wait! LTI 1.3 Platform configures a Tool with:
- `clientID`: Generated by the platform for the tool.
- `deploymentID`: Generated by the platform for the tool.
- `platformURL`: The issuer URL of the Platform.
- `keysetURL`: The JWKS URL of the Platform.
- `authURL`: The Auth URL of the Platform.
- `tokenURL`: The Token URL of the Platform.
- `privateKey`: The private key of the *Platform* to sign the LTI Launch `id_token` JWT.
If `LMSAdapter` holds this, it means Hanfledge IS the Platform and these are its own settings + the tool's `clientID` and `deploymentID`!
Wait, but what is the Tool's launch URL? Where is it in `LMSConfig`?
There is no `tool_url` or `initiation_url` or `target_link_uri` in `LMSConfig`!
Wait, `req.ActivityID` might be the Tool's URL?
```go
type LaunchRequest struct {
	UserID       string            `json:"user_id"`
	CourseID     string            `json:"course_id"`
	ActivityID   string            `json:"activity_id"`
...
```
Ah! Maybe `ActivityID` is the target link URI for the LTI tool!
And where is the Tool's OIDC login initiation URL?

Let's read the prompt carefully:
"Implement OIDC login initiation + JWT message signing"
"return nil, fmt.Errorf("LTI 1.3 LaunchURL not yet implemented")"

If the system is a Tool (since it's an LMSAdapter, maybe it adapts external LMSs for our Platform? Meaning our Platform acts as a Tool embedded in Canvas/Moodle?), then `platformURL` is Canvas's URL, `authURL` is Canvas's auth URL.
If our Platform acts as a Tool, how do we "Launch" it?
We don't. The LMS launches it.
Wait! "Third-Party Initiated Login". A Tool can initiate a launch by redirecting the user to the LMS's OIDC Auth URL!
To do that, the Tool creates a URL to the LMS's OIDC Authorization endpoint (which is `authURL`).
Wait, the prompt says "Implement OIDC login initiation + JWT message signing".
If we are doing "OIDC login initiation", we redirect to the LMS's `authURL`.
Where does "JWT message signing" come in?
If we sign a JWT, it means we are the **Platform** creating an `id_token` for an LTI Launch!
Let's see the standard: LTI 1.3 requires the Platform to sign an `id_token` with its private key and POST it to the Tool.
If Hanfledge is acting as the Platform, then we create the `id_token` and sign it with `privateKey`. We generate a form with `id_token` and `state`. The `URL` of the form would be the Tool's launch URL. Where is the Tool's launch URL? Maybe `req.ActivityID`?
Wait, the LTI 1.3 standard says:
OIDC login initiation is a request to the *Tool's* OIDC initiation endpoint.
Platform -> Tool (Init) -> Platform (Auth) -> Tool (Launch).

Let's look at `golang-jwt` in `go.mod`. It is present. `github.com/golang-jwt/jwt/v5`.
Let's look at `github.com/google/uuid`.

What if Hanfledge is the **Tool**, and we need to create an `LtiResourceLinkRequest`?
If Hanfledge is the Tool, we never generate an LTI Resource Link Request JWT. Only the Platform does that!
Wait! "Hanfledge is an AI-Native EdTech platform... Backend stack: Go (Gin, GORM)..."
Since Hanfledge is a Platform, it sends the LTI Launch (`LtiResourceLinkRequest`) to the Tool.
In LTI 1.3, the Platform MUST provide an OIDC Login Initiation request.
But wait! If `LaunchURL` generates a `LaunchResponse` with `Method: "POST"` and `FormData`, it's generating the final LTI launch form!
So "OIDC login initiation" might mean we are generating the OIDC authentication response (the `id_token`), bypassing the actual back-and-forth if the platform initiates it directly?
Or maybe `LaunchURL` generates the OIDC Login Initiation Request (which is a GET/POST to the Tool's OIDC Initiation URL)? But then where is the JWT signing coming from?
Wait, if Hanfledge is the **Tool**, and we are implementing the Deep Linking Response? No, `LaunchURL` is for launching an activity.

Let's search for `LTI 1.3` in the codebase to see how `LTI13Adapter` is used or documented.
