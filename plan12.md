Wait! Let's think if the prompt "OIDC login initiation + JWT message signing" means "Initiate the OIDC login" OR "Generate the OIDC login initiation URL AND generate the JWT message".
In LTI 1.3, the Platform sends an OIDC Login Initiation Request to the Tool's Initiation URL. The parameters are:
- `iss`: The Platform's issuer URL (`a.platformURL`)
- `login_hint`: User ID (`req.UserID`)
- `target_link_uri`: Activity ID (`req.ActivityID`)
- `lti_message_hint`: Course ID + Activity ID (`req.CourseID`)
- `client_id`: `a.clientID`
- `lti_deployment_id`: `a.deploymentID`

There is NO JWT signed in this request!
BUT the comment says "TODO: Implement OIDC login initiation + JWT message signing".
If we return the OIDC login initiation request, WHERE is the JWT message signing?
Maybe we generate the OIDC login initiation request to `a.authURL` (the Tool's Init URL) AND we pass a signed `id_token` as a `login_hint_token`? (Some tools support this).
Or maybe the "JWT message signing" is for the `state`? (Some platforms encode the state as a signed JWT to prevent tampering).
Or maybe the function ACTUALLY generates the final LTI Launch response (the `id_token`), bypassing the OIDC login initiation step entirely? "Simulated LTI Launch" or "IdP-Initiated Login".
"IdP-Initiated Login" in LTI 1.3:
The Platform (IdP) POSTs an `id_token` directly to the Tool's Launch URL (`target_link_uri`).
If we bypass the OIDC init and just generate the `id_token` directly, it completes the LTI 1.3 Launch in one step.
But the comment explicitly says "Implement OIDC login initiation + ...".
If it means the OIDC Request Object (`request` parameter), then Hanfledge is the Tool.

Let's assume Hanfledge is the **Platform** and `LaunchURL` implements the **OIDC Login Initiation Request** AND the **LTI Resource Link Request JWT**.
How can it return both? It can't. It returns ONE `LaunchResponse`.
Wait! What if Hanfledge is the Platform, and it returns a form POST to the Tool's **Launch URL** (`req.ActivityID`), with `id_token` (the signed JWT) AND `state` (generated by the Platform)? This is the standard "Auth Response" step.
But why does the comment mention "OIDC login initiation"?
Because the Auth Response is the *response* to the OIDC login initiation!
So "Implement [the response to] OIDC login initiation + JWT message signing [for the id_token]".

Let's write the code for generating the LTI 1.3 `id_token` JWT.

```go
package lms

import (
	"context"
	"fmt"
	"time"

	"github.com/golang-jwt/jwt/v5"
	"github.com/google/uuid"
)

func (a *LTI13Adapter) LaunchURL(ctx context.Context, req LaunchRequest) (*LaunchResponse, error) {
	if a.privateKey == "" {
		return nil, fmt.Errorf("LTI 1.3 private key is required for JWT signing")
	}

	key, err := jwt.ParseRSAPrivateKeyFromPEM([]byte(a.privateKey))
	if err != nil {
		return nil, fmt.Errorf("failed to parse LTI 1.3 private key: %w", err)
	}

	nonce := uuid.New().String()
	state := uuid.New().String()

	now := time.Now()
	claims := jwt.MapClaims{
		"iss":   a.platformURL,
		"aud":   a.clientID,
		"exp":   now.Add(5 * time.Minute).Unix(),
		"iat":   now.Unix(),
		"nonce": nonce,
		"sub":   req.UserID,
		"https://purl.imsglobal.org/spec/lti/claim/message_type":    "LtiResourceLinkRequest",
		"https://purl.imsglobal.org/spec/lti/claim/version":         "1.3.0",
		"https://purl.imsglobal.org/spec/lti/claim/deployment_id":   a.deploymentID,
		"https://purl.imsglobal.org/spec/lti/claim/target_link_uri": req.ActivityID,
		"https://purl.imsglobal.org/spec/lti/claim/resource_link": map[string]string{
			"id": req.ActivityID,
		},
		"https://purl.imsglobal.org/spec/lti/claim/context": map[string]string{
			"id": req.CourseID,
		},
	}

	if req.Role == "teacher" {
		claims["https://purl.imsglobal.org/spec/lti/claim/roles"] = []string{
			"http://purl.imsglobal.org/vocab/lis/v2/membership#Instructor",
		}
	} else {
		claims["https://purl.imsglobal.org/spec/lti/claim/roles"] = []string{
			"http://purl.imsglobal.org/vocab/lis/v2/membership#Learner",
		}
	}

	if len(req.CustomParams) > 0 {
		claims["https://purl.imsglobal.org/spec/lti/claim/custom"] = req.CustomParams
	}

	token := jwt.NewWithClaims(jwt.SigningMethodRS256, claims)
	token.Header["kid"] = "lti13-key" // Best practice

	signedToken, err := token.SignedString(key)
	if err != nil {
		return nil, fmt.Errorf("failed to sign LTI 1.3 JWT: %w", err)
	}

	// We return a POST to the ActivityID (acting as the Tool's launch URL)
	// or to a.authURL if that's the Tool's launch endpoint. Let's use a.authURL.
	targetURL := a.authURL
	if targetURL == "" {
		targetURL = req.ActivityID
	}

	return &LaunchResponse{
		URL:    targetURL,
		Method: "POST",
		FormData: map[string]string{
			"id_token": signedToken,
			"state":    state,
		},
		SessionID: state,
	}, nil
}
```

Wait, what if `LaunchURL` returns the OIDC Login Initiation parameters instead of `id_token`?
If `LaunchURL` is supposed to return the OIDC login initiation GET URL, then the "JWT message signing" might be referring to signing a `client_assertion` for the OIDC login initiation, OR generating the OIDC login initiation request AND signing a `login_hint_token`?
Actually, what if the `state` IS a signed JWT?
Sometimes Platforms encode `state` as a signed JWT to verify it hasn't been tampered with when the Tool redirects back.
If Hanfledge is the Platform, it sends the OIDC login initiation to the Tool:
URL: `a.authURL`
Params: `iss`, `login_hint`, `target_link_uri`, `lti_message_hint`, `client_id`, `lti_deployment_id`.
And then the Tool redirects back to the Platform's Auth URL with a `state`.
If Hanfledge is generating the OIDC login initiation, there is NO `state` parameter generated by the Platform. The Tool generates the `state`.
So the Platform doesn't generate a signed `state` for the OIDC login initiation.
Therefore, "JWT message signing" MUST mean generating the LTI Resource Link Request JWT (`id_token`)!
And to do "OIDC login initiation + JWT message signing" in one function, maybe we create an OIDC login initiation form POST, AND we also generate an `id_token` and put it in a custom parameter? No.
Maybe we just generate the final LTI launch `id_token` and POST it.
I'll implement the `id_token` generation as the primary logic, and return a POST. This makes sense for a single `LaunchURL` method.
Let's ask for plan review.
